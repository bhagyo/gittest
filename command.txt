git init

git status

git add first_file.txt   (to add a file)
git add -A  (to add all files of that folder)

git commit -m "first commit" filename.txt

git config --global user.email  "prachinmon1995@gmail.com"

git config --global user.name  "bhagyo"

git commit -m "the commit we want" file_name.txt

git remote add origin https://github.om/bhagyo/folderNmae

git push -u origin main